# V1 Scope And Deferrals

V1 focuses on a PowerShell-native DSL experience with New-WhyLog, local aliases I/W/E/P, top-level sequence YAML output, and parser-backed validation practices. Deferred items include robust alias-collision hardening and default progress-trace persistence, which can be added later if field usage shows clear value.